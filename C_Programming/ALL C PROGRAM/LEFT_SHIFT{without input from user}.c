#include <stdio.h>

int main() {
    printf("\n HARSH D \n");
    int num = 8; // Example number to be left shifted
    int shift = 2; // Number of bits to shift

    int result = num << shift;
    printf("Result after left shift: %d\n", result);

    return 0;
}
