import numpy as np
#arr=np.empty((3,2),dtype=int)
#arr=np.zeros((3,2),dtype=int)
#arr=np.ones((3,2),dtype=float)
arr= np.full((2, 2),'a')
print(arr)


'''
if we don't give dtype then default type will be float
'''
